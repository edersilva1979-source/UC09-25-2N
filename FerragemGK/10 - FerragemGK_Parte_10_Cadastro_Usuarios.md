<h1 align="center">Projeto FerragemGK</h1>

<div align="center">
<img src="Logo.png" width="200" alt="Exemplo">
</div>


# Parte 10: Cadastro de Usuários com FrmUsuario

Nesta etapa, nós vamos criar juntos a `FrmUsuario`, responsável pelo gerenciamento dos usuários que poderão acessar o FerragemGK.

Como estamos trabalhando com segurança e permissões, esse cadastro terá algumas regras diferentes dos cadastros de Cliente, Fornecedor e Produto.

Somente o usuário com nível:

```text
MASTER
```

poderá acessar esse módulo.

Nós vamos desenvolver:

* `Usuario.java`
* `UsuarioDAO.java`
* `FrmUsuario.java`
* Cadastro
* Consulta
* `JTable`
* Filtros
* Novo
* Salvar
* Alterar
* Excluir
* Ativar e desativar usuário
* Níveis MASTER e USER
* Validação de login duplicado
* Controle de permissão
* Proteção do usuário conectado

---

# 1. Estrutura do projeto

Nós já temos parte dessa estrutura por causa do Login.

Agora vamos ampliá la:

```text
FerragemGK

model
    Usuario.java

dao
    UsuarioDAO.java

util
    SessaoUsuario.java

view
    FrmLogin.java
    FrmPrincipal.java
    FrmUsuario.java
```

---

# 2. Tabela usuario

Nós já criamos:

```sql
CREATE TABLE usuario (
    id_usuario BIGINT GENERATED BY DEFAULT AS IDENTITY,
    nome VARCHAR(120) NOT NULL,
    login VARCHAR(60) NOT NULL,
    senha VARCHAR(255) NOT NULL,
    nivel VARCHAR(10) NOT NULL DEFAULT 'USER',
    ativo BOOLEAN NOT NULL DEFAULT TRUE,
    criado_em TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT pk_usuario
        PRIMARY KEY (id_usuario),

    CONSTRAINT uk_usuario_login
        UNIQUE (login),

    CONSTRAINT ck_usuario_nivel
        CHECK (
            nivel IN ('MASTER', 'USER')
        )
);
```

Nós teremos dois níveis:

```text
MASTER
USER
```

O `MASTER` terá acesso administrativo.

O `USER` utilizará os recursos operacionais permitidos.

---

# 3. Classe Usuario

Nós já utilizamos essa classe no Login.

Ela poderá permanecer assim:

```java
package model;

public class Usuario {

    private long idUsuario;
    private String nome;
    private String login;
    private String senha;
    private String nivel;
    private boolean ativo;

    public Usuario() {
    }

    public Usuario(
            long idUsuario,
            String nome,
            String login,
            String senha,
            String nivel,
            boolean ativo
    ) {

        this.idUsuario = idUsuario;
        this.nome = nome;
        this.login = login;
        this.senha = senha;
        this.nivel = nivel;
        this.ativo = ativo;
    }

    public long getIdUsuario() {
        return idUsuario;
    }

    public void setIdUsuario(long idUsuario) {
        this.idUsuario = idUsuario;
    }

    public String getNome() {
        return nome;
    }

    public void setNome(String nome) {
        this.nome = nome;
    }

    public String getLogin() {
        return login;
    }

    public void setLogin(String login) {
        this.login = login;
    }

    public String getSenha() {
        return senha;
    }

    public void setSenha(String senha) {
        this.senha = senha;
    }

    public String getNivel() {
        return nivel;
    }

    public void setNivel(String nivel) {
        this.nivel = nivel;
    }

    public boolean isAtivo() {
        return ativo;
    }

    public void setAtivo(boolean ativo) {
        this.ativo = ativo;
    }
}
```

---

# 4. Ampliando UsuarioDAO

Nossa `UsuarioDAO` já possui:

```java
autenticar()
```

Agora nós vamos acrescentar:

```text
cadastrar
alterar
excluir
buscarPorId
listarTodos
pesquisar
loginExiste
```

---

# 5. Imports da UsuarioDAO

```java
package dao;

import conexao.Conexao;
import model.Usuario;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.SQLException;

import java.util.ArrayList;
import java.util.List;
```

---

# 6. Método cadastrar

```java
public long cadastrar(
        Usuario usuario
) {

    String sql =
            "INSERT INTO usuario ("
            + "nome, "
            + "login, "
            + "senha, "
            + "nivel, "
            + "ativo"
            + ") VALUES (?, ?, ?, ?, ?) "
            + "RETURNING id_usuario";

    try (
            Connection conexao =
                    Conexao.conectar();

            PreparedStatement stmt =
                    conexao.prepareStatement(sql)
    ) {

        stmt.setString(1, usuario.getNome());
        stmt.setString(2, usuario.getLogin());
        stmt.setString(3, usuario.getSenha());
        stmt.setString(4, usuario.getNivel());
        stmt.setBoolean(5, usuario.isAtivo());

        try (
                ResultSet rs =
                        stmt.executeQuery()
        ) {

            if (rs.next()) {
                return rs.getLong("id_usuario");
            }
        }

    } catch (SQLException erro) {

        throw new RuntimeException(
                "Erro ao cadastrar usuário.",
                erro
        );
    }

    return 0;
}
```

---

# 7. Verificando login duplicado

Nós não podemos ter dois usuários com o mesmo login.

```java
public boolean loginExiste(
        String login,
        long idUsuario
) {

    String sql =
            "SELECT 1 "
            + "FROM usuario "
            + "WHERE LOWER(login) = LOWER(?) "
            + "AND id_usuario <> ?";

    try (
            Connection conexao =
                    Conexao.conectar();

            PreparedStatement stmt =
                    conexao.prepareStatement(sql)
    ) {

        stmt.setString(1, login);
        stmt.setLong(2, idUsuario);

        try (
                ResultSet rs =
                        stmt.executeQuery()
        ) {
            return rs.next();
        }

    } catch (SQLException erro) {

        throw new RuntimeException(
                "Erro ao verificar login.",
                erro
        );
    }
}
```

Quando estivermos cadastrando um novo usuário:

```text
idUsuario = 0
```

Quando estivermos alterando:

```text
idUsuario = código do usuário
```

---

# 8. Alterando usuário

Se a senha ficar vazia durante a alteração, nós vamos manter a senha atual.

Se uma nova senha for informada, nós vamos atualizá la.

```java
public boolean alterar(
        Usuario usuario
) {

    boolean alterarSenha =
            usuario.getSenha() != null
            && !usuario
                    .getSenha()
                    .isBlank();

    String sql;

    if (alterarSenha) {

        sql =
                "UPDATE usuario SET "
                + "nome = ?, "
                + "login = ?, "
                + "senha = ?, "
                + "nivel = ?, "
                + "ativo = ? "
                + "WHERE id_usuario = ?";

    } else {

        sql =
                "UPDATE usuario SET "
                + "nome = ?, "
                + "login = ?, "
                + "nivel = ?, "
                + "ativo = ? "
                + "WHERE id_usuario = ?";
    }

    try (
            Connection conexao =
                    Conexao.conectar();

            PreparedStatement stmt =
                    conexao.prepareStatement(sql)
    ) {

        if (alterarSenha) {

            stmt.setString(1, usuario.getNome());
            stmt.setString(2, usuario.getLogin());
            stmt.setString(3, usuario.getSenha());
            stmt.setString(4, usuario.getNivel());
            stmt.setBoolean(5, usuario.isAtivo());
            stmt.setLong(6, usuario.getIdUsuario());

        } else {

            stmt.setString(1, usuario.getNome());
            stmt.setString(2, usuario.getLogin());
            stmt.setString(3, usuario.getNivel());
            stmt.setBoolean(4, usuario.isAtivo());
            stmt.setLong(5, usuario.getIdUsuario());
        }

        return stmt.executeUpdate() > 0;

    } catch (SQLException erro) {

        throw new RuntimeException(
                "Erro ao alterar usuário.",
                erro
        );
    }
}
```

Essa solução evita mostrar a senha atual na tela.

---

# 9. Buscando usuário pelo ID

```java
public Usuario buscarPorId(
        long idUsuario
) {

    String sql =
            "SELECT "
            + "id_usuario, "
            + "nome, "
            + "login, "
            + "nivel, "
            + "ativo "
            + "FROM usuario "
            + "WHERE id_usuario = ?";

    try (
            Connection conexao =
                    Conexao.conectar();

            PreparedStatement stmt =
                    conexao.prepareStatement(sql)
    ) {

        stmt.setLong(1, idUsuario);

        try (
                ResultSet rs =
                        stmt.executeQuery()
        ) {

            if (rs.next()) {
                return montarUsuario(rs);
            }
        }

    } catch (SQLException erro) {

        throw new RuntimeException(
                "Erro ao localizar usuário.",
                erro
        );
    }

    return null;
}
```

Observe que nós não estamos buscando a senha. Isso é proposital.

---

# 10. Método montarUsuario

```java
private Usuario montarUsuario(
        ResultSet rs
) throws SQLException {

    Usuario usuario =
            new Usuario();

    usuario.setIdUsuario(
            rs.getLong("id_usuario")
    );

    usuario.setNome(
            rs.getString("nome")
    );

    usuario.setLogin(
            rs.getString("login")
    );

    usuario.setNivel(
            rs.getString("nivel")
    );

    usuario.setAtivo(
            rs.getBoolean("ativo")
    );

    return usuario;
}
```

---

# 11. Listando todos os usuários

```java
public List<Usuario> listarTodos() {

    List<Usuario> usuarios =
            new ArrayList<>();

    String sql =
            "SELECT "
            + "id_usuario, "
            + "nome, "
            + "login, "
            + "nivel, "
            + "ativo "
            + "FROM usuario "
            + "ORDER BY nome";

    try (
            Connection conexao =
                    Conexao.conectar();

            PreparedStatement stmt =
                    conexao.prepareStatement(sql);

            ResultSet rs =
                    stmt.executeQuery()
    ) {

        while (rs.next()) {
            usuarios.add(
                    montarUsuario(rs)
            );
        }

    } catch (SQLException erro) {

        throw new RuntimeException(
                "Erro ao listar usuários.",
                erro
        );
    }

    return usuarios;
}
```

---

# 12. Pesquisando usuários

Nós vamos permitir pesquisa por:

```text
NOME
LOGIN
NIVEL
ID
```

```java
public List<Usuario> pesquisar(
        String filtro,
        String pesquisa
) {

    List<Usuario> usuarios =
            new ArrayList<>();

    String sql;

    switch (filtro) {

        case "ID":

            sql =
                    "SELECT id_usuario, nome, login, nivel, ativo "
                    + "FROM usuario "
                    + "WHERE id_usuario = ? "
                    + "ORDER BY nome";

            break;

        case "LOGIN":

            sql =
                    "SELECT id_usuario, nome, login, nivel, ativo "
                    + "FROM usuario "
                    + "WHERE login ILIKE ? "
                    + "ORDER BY nome";

            break;

        case "NIVEL":

            sql =
                    "SELECT id_usuario, nome, login, nivel, ativo "
                    + "FROM usuario "
                    + "WHERE nivel ILIKE ? "
                    + "ORDER BY nome";

            break;

        default:

            sql =
                    "SELECT id_usuario, nome, login, nivel, ativo "
                    + "FROM usuario "
                    + "WHERE nome ILIKE ? "
                    + "ORDER BY nome";

            break;
    }

    try (
            Connection conexao =
                    Conexao.conectar();

            PreparedStatement stmt =
                    conexao.prepareStatement(sql)
    ) {

        if ("ID".equals(filtro)) {

            stmt.setLong(
                    1,
                    Long.parseLong(pesquisa)
            );

        } else {

            stmt.setString(
                    1,
                    "%" + pesquisa + "%"
            );
        }

        try (
                ResultSet rs =
                        stmt.executeQuery()
        ) {

            while (rs.next()) {
                usuarios.add(
                        montarUsuario(rs)
                );
            }
        }

    } catch (SQLException erro) {

        throw new RuntimeException(
                "Erro ao pesquisar usuários.",
                erro
        );
    }

    return usuarios;
}
```

---

# 13. Excluindo usuário

```java
public boolean excluir(
        long idUsuario
) {

    String sql =
            "DELETE FROM usuario "
            + "WHERE id_usuario = ?";

    try (
            Connection conexao =
                    Conexao.conectar();

            PreparedStatement stmt =
                    conexao.prepareStatement(sql)
    ) {

        stmt.setLong(1, idUsuario);

        return stmt.executeUpdate() > 0;

    } catch (SQLException erro) {

        throw new RuntimeException(
                "Não foi possível excluir o usuário.",
                erro
        );
    }
}
```

Se o usuário já realizou Compras ou Vendas, o PostgreSQL poderá impedir a exclusão por causa das chaves estrangeiras.

Nesse caso, a melhor solução será:

```text
Ativo = false
```

Isso preserva o histórico.

---

# 14. Criando a FrmUsuario

No pacote `view`, crie:

```text
FrmUsuario
```

como:

```text
JInternalFrame Form
```

---

# 15. Configurações da tela

No NetBeans:

```text
Title
Cadastro de Usuários

Closable
true

Iconifiable
true

Maximizable
true

Resizable
true
```

---

# 16. Criando JTabbedPane

Vamos criar:

```text
JTabbedPane
```

Nome:

```text
tabUsuario
```

Com duas abas:

```text
Cadastro
Consulta
```

---

# 17. Aba Cadastro

Nós teremos:

```text
Código
Nome
Login
Senha
Nível
Ativo
```

Variáveis:

```text
txtCodigo
txtNome
txtLogin
txtSenha
cmbNivel
chkAtivo
```

Para senha, utilize:

```text
JPasswordField
```

---

# 18. JComboBox de nível

O `cmbNivel` terá:

```text
USER
MASTER
```

Podemos deixar `USER` como valor padrão.

---

# 19. Botões

Na aba Cadastro:

```text
Novo
Salvar
Alterar
Excluir
Cancelar
```

Variáveis:

```text
btnNovo
btnSalvar
btnAlterar
btnExcluir
btnCancelar
```

---

# 20. Aba Consulta

Crie:

```text
Filtro
Pesquisa
Localizar
Listar Todos
Carregar Selecionado
JTable
```

Variáveis:

```text
cmbFiltro
txtPesquisa
btnLocalizar
btnListarTodos
btnCarregar
tblUsuarios
```

---

# 21. Filtros

No `cmbFiltro`:

```text
NOME
LOGIN
NIVEL
ID
```

---

# 22. Colunas da JTable

Nós vamos exibir:

```text
Código
Nome
Login
Nível
Ativo
```

A senha nunca deverá aparecer na tabela.

---

# 23. Imports da FrmUsuario

```java
import dao.UsuarioDAO;

import java.util.List;

import javax.swing.JOptionPane;
import javax.swing.table.DefaultTableModel;

import model.Usuario;
import util.SessaoUsuario;
```

---

# 24. Variáveis da tela

```java
private final UsuarioDAO usuarioDAO =
        new UsuarioDAO();

private long idUsuarioSelecionado = 0;
```

---

# 25. Construtor

```java
public FrmUsuario() {

    initComponents();

    configurarTela();

    listarUsuarios();

    limparCampos();
}
```

---

# 26. Protegendo a tela

```java
private void configurarTela() {

    if (!SessaoUsuario.isMaster()) {

        JOptionPane.showMessageDialog(
                this,
                "Acesso permitido somente para usuário MASTER."
        );

        dispose();
        return;
    }

    txtCodigo.setEditable(false);

    tblUsuarios.setModel(
            criarModeloTabela()
    );

    tblUsuarios.setSelectionMode(
            javax.swing.ListSelectionModel
                    .SINGLE_SELECTION
    );

    tblUsuarios.setAutoCreateRowSorter(true);
}
```

---

# 27. Modelo da JTable

```java
private DefaultTableModel criarModeloTabela() {

    return new DefaultTableModel(
            new Object[]{
                "Código",
                "Nome",
                "Login",
                "Nível",
                "Ativo"
            },
            0
    ) {

        @Override
        public boolean isCellEditable(
                int row,
                int column
        ) {
            return false;
        }
    };
}
```

---

# 28. Preenchendo a tabela

```java
private void preencherTabela(
        List<Usuario> usuarios
) {

    DefaultTableModel modelo =
            (DefaultTableModel)
            tblUsuarios.getModel();

    modelo.setRowCount(0);

    for (Usuario usuario : usuarios) {

        modelo.addRow(
                new Object[]{
                    usuario.getIdUsuario(),
                    usuario.getNome(),
                    usuario.getLogin(),
                    usuario.getNivel(),
                    usuario.isAtivo()
                            ? "Sim"
                            : "Não"
                }
        );
    }
}
```

---

# 29. Listando usuários

```java
private void listarUsuarios() {

    try {

        preencherTabela(
                usuarioDAO.listarTodos()
        );

    } catch (Exception erro) {

        JOptionPane.showMessageDialog(
                this,
                "Erro ao carregar usuários.\n"
                + erro.getMessage()
        );
    }
}
```

---

# 30. Limpando campos

```java
private void limparCampos() {

    idUsuarioSelecionado = 0;

    txtCodigo.setText("");
    txtNome.setText("");
    txtLogin.setText("");
    txtSenha.setText("");

    cmbNivel.setSelectedItem("USER");

    chkAtivo.setSelected(true);

    txtNome.requestFocus();
}
```

---

# 31. Validando os campos

```java
private boolean validarCampos(
        boolean novoUsuario
) {

    if (
            txtNome
            .getText()
            .trim()
            .isEmpty()
    ) {

        JOptionPane.showMessageDialog(
                this,
                "Informe o nome."
        );

        txtNome.requestFocus();
        return false;
    }

    if (
            txtLogin
            .getText()
            .trim()
            .isEmpty()
    ) {

        JOptionPane.showMessageDialog(
                this,
                "Informe o login."
        );

        txtLogin.requestFocus();
        return false;
    }

    if (
            novoUsuario
            && txtSenha
                    .getPassword()
                    .length == 0
    ) {

        JOptionPane.showMessageDialog(
                this,
                "Informe a senha."
        );

        txtSenha.requestFocus();
        return false;
    }

    if (
            usuarioDAO.loginExiste(
                    txtLogin
                    .getText()
                    .trim(),
                    idUsuarioSelecionado
            )
    ) {

        JOptionPane.showMessageDialog(
                this,
                "Este login já está sendo utilizado."
        );

        txtLogin.requestFocus();
        return false;
    }

    return true;
}
```

---

# 32. Criando Usuario a partir dos campos

```java
private Usuario criarUsuarioComCampos() {

    Usuario usuario =
            new Usuario();

    usuario.setIdUsuario(
            idUsuarioSelecionado
    );

    usuario.setNome(
            txtNome.getText().trim()
    );

    usuario.setLogin(
            txtLogin.getText().trim()
    );

    usuario.setSenha(
            new String(
                    txtSenha.getPassword()
            )
    );

    usuario.setNivel(
            cmbNivel
            .getSelectedItem()
            .toString()
    );

    usuario.setAtivo(
            chkAtivo.isSelected()
    );

    return usuario;
}
```

---

# 33. Botão Novo

```java
private void btnNovoActionPerformed(
        java.awt.event.ActionEvent evt
) {

    limparCampos();

    tabUsuario.setSelectedIndex(0);
}
```

---

# 34. Botão Salvar

```java
private void btnSalvarActionPerformed(
        java.awt.event.ActionEvent evt
) {

    if (idUsuarioSelecionado != 0) {

        JOptionPane.showMessageDialog(
                this,
                "Existe um usuário carregado para edição.\n"
                + "Utilize Alterar ou clique em Novo."
        );

        return;
    }

    if (!validarCampos(true)) {
        return;
    }

    try {

        Usuario usuario =
                criarUsuarioComCampos();

        long codigo =
                usuarioDAO.cadastrar(usuario);

        JOptionPane.showMessageDialog(
                this,
                "Usuário cadastrado com sucesso.\n"
                + "Código: "
                + codigo
        );

        limparCampos();
        listarUsuarios();

    } catch (Exception erro) {

        JOptionPane.showMessageDialog(
                this,
                "Não foi possível cadastrar o usuário.\n"
                + erro.getMessage()
        );
    }
}
```

---

# 35. Botão Localizar

```java
private void btnLocalizarActionPerformed(
        java.awt.event.ActionEvent evt
) {

    String filtro =
            cmbFiltro
            .getSelectedItem()
            .toString();

    String pesquisa =
            txtPesquisa
            .getText()
            .trim();

    if (pesquisa.isEmpty()) {
        listarUsuarios();
        return;
    }

    if ("ID".equals(filtro)) {

        try {
            Long.parseLong(pesquisa);

        } catch (NumberFormatException erro) {

            JOptionPane.showMessageDialog(
                    this,
                    "Para pesquisar por ID informe apenas números."
            );

            return;
        }
    }

    try {

        List<Usuario> usuarios =
                usuarioDAO.pesquisar(
                        filtro,
                        pesquisa
                );

        preencherTabela(usuarios);

        if (usuarios.isEmpty()) {

            JOptionPane.showMessageDialog(
                    this,
                    "Nenhum usuário encontrado."
            );
        }

    } catch (Exception erro) {

        JOptionPane.showMessageDialog(
                this,
                "Erro na pesquisa.\n"
                + erro.getMessage()
        );
    }
}
```

---

# 36. Botão Listar Todos

```java
private void btnListarTodosActionPerformed(
        java.awt.event.ActionEvent evt
) {

    txtPesquisa.setText("");
    listarUsuarios();
}
```

---

# 37. Carregando usuário selecionado

```java
private void carregarUsuarioSelecionado() {

    int linha =
            tblUsuarios.getSelectedRow();

    if (linha < 0) {

        JOptionPane.showMessageDialog(
                this,
                "Selecione um usuário."
        );

        return;
    }

    int linhaModelo =
            tblUsuarios.convertRowIndexToModel(
                    linha
            );

    long idUsuario =
            Long.parseLong(
                    tblUsuarios
                    .getModel()
                    .getValueAt(
                            linhaModelo,
                            0
                    )
                    .toString()
            );

    try {

        Usuario usuario =
                usuarioDAO.buscarPorId(
                        idUsuario
                );

        if (usuario == null) {

            JOptionPane.showMessageDialog(
                    this,
                    "Usuário não encontrado."
            );

            return;
        }

        preencherCampos(usuario);

        tabUsuario.setSelectedIndex(0);

    } catch (Exception erro) {

        JOptionPane.showMessageDialog(
                this,
                "Erro ao carregar usuário.\n"
                + erro.getMessage()
        );
    }
}
```

---

# 38. Preenchendo os campos

```java
private void preencherCampos(
        Usuario usuario
) {

    idUsuarioSelecionado =
            usuario.getIdUsuario();

    txtCodigo.setText(
            String.valueOf(
                    usuario.getIdUsuario()
            )
    );

    txtNome.setText(
            usuario.getNome()
    );

    txtLogin.setText(
            usuario.getLogin()
    );

    txtSenha.setText("");

    cmbNivel.setSelectedItem(
            usuario.getNivel()
    );

    chkAtivo.setSelected(
            usuario.isAtivo()
    );
}
```

Observe que nós não carregamos a senha.

Se o campo senha permanecer vazio durante a alteração:

```text
senha atual é mantida
```

Se uma nova senha for digitada:

```text
senha é atualizada
```

---

# 39. Botão Carregar

```java
private void btnCarregarActionPerformed(
        java.awt.event.ActionEvent evt
) {

    carregarUsuarioSelecionado();
}
```

Também podemos utilizar duplo clique:

```java
private void tblUsuariosMouseClicked(
        java.awt.event.MouseEvent evt
) {

    if (evt.getClickCount() == 2) {
        carregarUsuarioSelecionado();
    }
}
```

---

# 40. Botão Alterar

```java
private void btnAlterarActionPerformed(
        java.awt.event.ActionEvent evt
) {

    if (idUsuarioSelecionado == 0) {

        JOptionPane.showMessageDialog(
                this,
                "Localize e carregue um usuário antes de alterar."
        );

        return;
    }

    if (!validarCampos(false)) {
        return;
    }

    Usuario usuarioLogado =
            SessaoUsuario.getUsuarioLogado();

    if (
            usuarioLogado != null
            && usuarioLogado.getIdUsuario()
            == idUsuarioSelecionado
            && !chkAtivo.isSelected()
    ) {

        JOptionPane.showMessageDialog(
                this,
                "Você não pode desativar o usuário que está conectado."
        );

        return;
    }

    if (
            usuarioLogado != null
            && usuarioLogado.getIdUsuario()
            == idUsuarioSelecionado
            && !"MASTER".equals(
                    cmbNivel
                    .getSelectedItem()
                    .toString()
            )
    ) {

        JOptionPane.showMessageDialog(
                this,
                "Você não pode retirar sua própria permissão MASTER durante a sessão."
        );

        return;
    }

    int resposta =
            JOptionPane.showConfirmDialog(
                    this,
                    "Deseja salvar as alterações deste usuário?",
                    "Alterar Usuário",
                    JOptionPane.YES_NO_OPTION
            );

    if (resposta != JOptionPane.YES_OPTION) {
        return;
    }

    try {

        Usuario usuario =
                criarUsuarioComCampos();

        boolean alterado =
                usuarioDAO.alterar(usuario);

        if (alterado) {

            JOptionPane.showMessageDialog(
                    this,
                    "Usuário alterado com sucesso."
            );

            limparCampos();
            listarUsuarios();
        }

    } catch (Exception erro) {

        JOptionPane.showMessageDialog(
                this,
                "Não foi possível alterar o usuário.\n"
                + erro.getMessage()
        );
    }
}
```

---

# 41. Botão Excluir

```java
private void btnExcluirActionPerformed(
        java.awt.event.ActionEvent evt
) {

    if (!SessaoUsuario.isMaster()) {

        JOptionPane.showMessageDialog(
                this,
                "Somente MASTER pode excluir usuários."
        );

        return;
    }

    if (idUsuarioSelecionado == 0) {

        JOptionPane.showMessageDialog(
                this,
                "Selecione um usuário."
        );

        return;
    }

    Usuario usuarioLogado =
            SessaoUsuario.getUsuarioLogado();

    if (
            usuarioLogado != null
            && usuarioLogado.getIdUsuario()
            == idUsuarioSelecionado
    ) {

        JOptionPane.showMessageDialog(
                this,
                "Você não pode excluir o usuário que está conectado."
        );

        return;
    }

    int resposta =
            JOptionPane.showConfirmDialog(
                    this,
                    "Deseja realmente excluir este usuário?",
                    "Excluir Usuário",
                    JOptionPane.YES_NO_OPTION
            );

    if (resposta != JOptionPane.YES_OPTION) {
        return;
    }

    try {

        boolean excluido =
                usuarioDAO.excluir(
                        idUsuarioSelecionado
                );

        if (excluido) {

            JOptionPane.showMessageDialog(
                    this,
                    "Usuário excluído com sucesso."
            );

            limparCampos();
            listarUsuarios();
        }

    } catch (Exception erro) {

        JOptionPane.showMessageDialog(
                this,
                "Não foi possível excluir o usuário.\n"
                + "Ele pode possuir compras ou vendas vinculadas.\n"
                + "Nesse caso, utilize a opção Ativo para desativá lo."
        );
    }
}
```

---

# 42. Botão Cancelar

```java
private void btnCancelarActionPerformed(
        java.awt.event.ActionEvent evt
) {

    limparCampos();
}
```

---

# 43. Ligando FrmUsuario à FrmPrincipal

Na `FrmPrincipal`:

```java
private void mnuUsuariosActionPerformed(
        java.awt.event.ActionEvent evt
) {

    if (!SessaoUsuario.isMaster()) {

        JOptionPane.showMessageDialog(
                this,
                "Acesso permitido somente para usuário MASTER."
        );

        return;
    }

    abrirTela(
            new FrmUsuario()
    );
}
```

---

# 44. Fluxo do cadastro

```text
FrmPrincipal
    ↓
Cadastros
    ↓
Usuários
    ↓
FrmUsuario
    ↓
Cadastro
    ↓
Salvar
    ↓
Usuario
    ↓
UsuarioDAO
    ↓
PostgreSQL
```

---

# 45. Fluxo da alteração

```text
Consulta
    ↓
Selecionar usuário
    ↓
Carregar
    ↓
Alterar informações
    ↓
Senha vazia?
    ↓
SIM
    ↓
Mantém senha atual
```

Ou:

```text
Nova senha informada
    ↓
Atualiza senha
```

---

# 46. Ativando e desativando usuários

Nós não precisamos excluir um usuário para impedir seu acesso.

Basta alterar:

```text
Ativo
```

para:

```text
false
```

Como nosso método de autenticação possui:

```sql
AND ativo = TRUE
```

o usuário desativado não conseguirá mais entrar no sistema.

Essa é normalmente uma solução melhor do que apagar um usuário que já possui histórico.

---

# 47. Testando novo MASTER

Cadastre:

```text
Nome
Administrador 2

Login
admin2

Senha
1234

Nível
MASTER

Ativo
Sim
```

Depois faça logout e teste o acesso.

---

# 48. Testando USER

Cadastre:

```text
Nome
Operador Caixa

Login
caixa

Senha
1234

Nível
USER

Ativo
Sim
```

Depois faça login com esse usuário.

O menu:

```text
Usuários
```

deverá ficar bloqueado.

Também permanecerão as demais restrições que nós já criamos para USER.

---

# 49. Testando usuário inativo

Altere:

```text
Ativo
Não
```

Depois tente fazer login.

O sistema deverá retornar:

```text
Usuário ou senha inválidos.
```

---

# 50. Segurança das senhas

Na versão didática inicial do FerragemGK nós estamos armazenando a senha diretamente no banco.

Por exemplo:

```text
1234
```

Isso facilita o aprendizado de:

```text
Login
JDBC
PreparedStatement
DAO
Sessão
Permissões
```

Porém essa abordagem não deve ser utilizada em uma aplicação real.

Em uma evolução do projeto, devemos substituir a senha armazenada por:

```text
hash de senha
```

Utilizando algoritmos apropriados para armazenamento de credenciais.

O `JPasswordField` apenas esconde visualmente os caracteres digitados.

Ele não protege a senha armazenada no banco.

---

# 51. Resultado desta etapa

Com a `FrmUsuario`, nosso FerragemGK passa a possuir um módulo administrativo completo para:

```text
Cadastrar usuários
Consultar usuários
Pesquisar
Alterar dados
Alterar senha
Ativar usuário
Desativar usuário
Excluir quando permitido
Controlar MASTER
Controlar USER
Impedir login duplicado
Impedir exclusão do próprio usuário
Impedir desativação do usuário conectado
Impedir perda da própria permissão MASTER
Preservar senhas durante alterações
```

---

# 52. Arquitetura do módulo

```text
FrmUsuario
    ↓
Usuario
    ↓
UsuarioDAO
    ↓
Conexao
    ↓
PostgreSQL
```

Além disso:

```text
SessaoUsuario
    ↓
Controle de acesso
    ↓
MASTER / USER
```

---

# 53. Próxima evolução

Depois da `FrmUsuario`, nosso módulo administrativo estará praticamente completo.

Uma evolução interessante será melhorar a segurança da autenticação utilizando:

```text
Hash de senha
```

e também poderemos criar:

```text
Alteração de senha pelo próprio usuário
Auditoria de acessos
Registro de data e hora do último login
Controle de tentativas de acesso
```

Essas melhorias podem ser aplicadas depois que concluirmos toda a parte funcional principal do FerragemGK.
