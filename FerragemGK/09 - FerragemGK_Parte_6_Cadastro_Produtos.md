# Projeto FerragemGK

# Parte 6: Cadastro de Produtos com CRUD Completo

Nesta etapa, nós vamos criar juntos o cadastro de produtos do FerragemGK.

A `FrmProduto` seguirá o mesmo padrão visual e estrutural que nós já utilizamos em `FrmCliente` e `FrmFornecedor`.

Agora, porém, nós vamos acrescentar novos conceitos importantes para um sistema comercial:

* Preço de custo
* Preço de venda
* Unidade de medida
* Estoque atual
* Estoque mínimo
* Produto ativo
* Pesquisa de produtos
* Identificação de estoque baixo
* Controle de permissão para exclusão

Ao final desta etapa, nós teremos o cadastro de produtos pronto para ser utilizado posteriormente nos módulos de Compra e Venda.

---

# 1. Estrutura do projeto

Depois desta etapa, nossa estrutura ficará semelhante a:

```text
FerragemGK

conexao
    Conexao.java

model
    Usuario.java
    Cliente.java
    Fornecedor.java
    Produto.java

dao
    UsuarioDAO.java
    ClienteDAO.java
    FornecedorDAO.java
    ProdutoDAO.java

util
    SessaoUsuario.java

view
    FrmLogin.java
    FrmPrincipal.java
    FrmCliente.java
    FrmFornecedor.java
    FrmProduto.java
```

---

# 2. Tabela produto

No PostgreSQL nós já criamos:

```sql
CREATE TABLE produto (
    id_produto BIGINT GENERATED BY DEFAULT AS IDENTITY,
    descricao VARCHAR(180) NOT NULL,
    unidade VARCHAR(10) NOT NULL DEFAULT 'UN',
    preco_custo NUMERIC(12,2) NOT NULL DEFAULT 0,
    preco_venda NUMERIC(12,2) NOT NULL DEFAULT 0,
    estoque NUMERIC(12,3) NOT NULL DEFAULT 0,
    estoque_minimo NUMERIC(12,3) NOT NULL DEFAULT 0,
    ativo BOOLEAN NOT NULL DEFAULT TRUE,
    criado_em TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT pk_produto
        PRIMARY KEY (id_produto),

    CONSTRAINT ck_produto_preco_custo
        CHECK (preco_custo >= 0),

    CONSTRAINT ck_produto_preco_venda
        CHECK (preco_venda >= 0),

    CONSTRAINT ck_produto_estoque
        CHECK (estoque >= 0),

    CONSTRAINT ck_produto_estoque_minimo
        CHECK (estoque_minimo >= 0)
);
```

Essa tabela será uma das mais importantes do projeto.

Mais adiante ela será utilizada em:

```text
Compra
Venda
Produtos da Compra
Produtos da Venda
Controle de estoque
Consulta de estoque mínimo
```

---

# 3. Por que nós vamos utilizar BigDecimal

Para valores monetários nós não vamos utilizar `double`.

Nós vamos utilizar:

```java
BigDecimal
```

Isso será importante principalmente para:

```text
Preço de custo
Preço de venda
Valores de compra
Valores de venda
Parcelas
Contas a pagar
Contas a receber
```

O `BigDecimal` é mais adequado quando precisamos trabalhar com valores que exigem precisão decimal.

Nós também vamos utilizá lo no estoque, porque nosso banco permite até três casas decimais.

Por exemplo:

```text
1
1,500
2,750
10,250
```

Isso permite trabalhar futuramente com unidades que não sejam apenas números inteiros.

---

# 4. Criando Produto.java

Dentro do pacote:

```text
model
```

nós vamos criar:

```text
Produto.java
```

Código completo:

```java
package model;

import java.math.BigDecimal;

public class Produto {

    private long idProduto;
    private String descricao;
    private String unidade;
    private BigDecimal precoCusto;
    private BigDecimal precoVenda;
    private BigDecimal estoque;
    private BigDecimal estoqueMinimo;
    private boolean ativo;

    public Produto() {
    }

    public long getIdProduto() {
        return idProduto;
    }

    public void setIdProduto(long idProduto) {
        this.idProduto = idProduto;
    }

    public String getDescricao() {
        return descricao;
    }

    public void setDescricao(String descricao) {
        this.descricao = descricao;
    }

    public String getUnidade() {
        return unidade;
    }

    public void setUnidade(String unidade) {
        this.unidade = unidade;
    }

    public BigDecimal getPrecoCusto() {
        return precoCusto;
    }

    public void setPrecoCusto(BigDecimal precoCusto) {
        this.precoCusto = precoCusto;
    }

    public BigDecimal getPrecoVenda() {
        return precoVenda;
    }

    public void setPrecoVenda(BigDecimal precoVenda) {
        this.precoVenda = precoVenda;
    }

    public BigDecimal getEstoque() {
        return estoque;
    }

    public void setEstoque(BigDecimal estoque) {
        this.estoque = estoque;
    }

    public BigDecimal getEstoqueMinimo() {
        return estoqueMinimo;
    }

    public void setEstoqueMinimo(BigDecimal estoqueMinimo) {
        this.estoqueMinimo = estoqueMinimo;
    }

    public boolean isAtivo() {
        return ativo;
    }

    public void setAtivo(boolean ativo) {
        this.ativo = ativo;
    }
}
```

---

# 5. Criando ProdutoDAO

Agora nós vamos criar:

```text
dao
    ProdutoDAO.java
```

Imports:

```java
package dao;

import conexao.Conexao;
import model.Produto;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.SQLException;

import java.util.ArrayList;
import java.util.List;
```

Dentro da classe nós vamos criar:

```text
cadastrar
alterar
excluir
buscarPorId
listarTodos
pesquisar
listarEstoqueBaixo
montarProduto
```

---

# 6. Método cadastrar

Nós vamos criar:

```java
public long cadastrar(
        Produto produto
) {

    String sql =
            "INSERT INTO produto ("
            + "descricao, "
            + "unidade, "
            + "preco_custo, "
            + "preco_venda, "
            + "estoque, "
            + "estoque_minimo, "
            + "ativo"
            + ") VALUES ("
            + "?, ?, ?, ?, ?, ?, ?"
            + ") "
            + "RETURNING id_produto";

    try (
            Connection conexao =
                    Conexao.conectar();

            PreparedStatement stmt =
                    conexao.prepareStatement(sql)
    ) {

        stmt.setString(
                1,
                produto.getDescricao()
        );

        stmt.setString(
                2,
                produto.getUnidade()
        );

        stmt.setBigDecimal(
                3,
                produto.getPrecoCusto()
        );

        stmt.setBigDecimal(
                4,
                produto.getPrecoVenda()
        );

        stmt.setBigDecimal(
                5,
                produto.getEstoque()
        );

        stmt.setBigDecimal(
                6,
                produto.getEstoqueMinimo()
        );

        stmt.setBoolean(
                7,
                produto.isAtivo()
        );

        try (
                ResultSet rs =
                        stmt.executeQuery()
        ) {

            if (rs.next()) {

                return rs.getLong(
                        "id_produto"
                );
            }
        }

    } catch (SQLException erro) {

        throw new RuntimeException(
                "Erro ao cadastrar produto.",
                erro
        );
    }

    return 0;
}
```

---

# 7. Método alterar

```java
public boolean alterar(
        Produto produto
) {

    String sql =
            "UPDATE produto SET "
            + "descricao = ?, "
            + "unidade = ?, "
            + "preco_custo = ?, "
            + "preco_venda = ?, "
            + "estoque = ?, "
            + "estoque_minimo = ?, "
            + "ativo = ? "
            + "WHERE id_produto = ?";

    try (
            Connection conexao =
                    Conexao.conectar();

            PreparedStatement stmt =
                    conexao.prepareStatement(sql)
    ) {

        stmt.setString(
                1,
                produto.getDescricao()
        );

        stmt.setString(
                2,
                produto.getUnidade()
        );

        stmt.setBigDecimal(
                3,
                produto.getPrecoCusto()
        );

        stmt.setBigDecimal(
                4,
                produto.getPrecoVenda()
        );

        stmt.setBigDecimal(
                5,
                produto.getEstoque()
        );

        stmt.setBigDecimal(
                6,
                produto.getEstoqueMinimo()
        );

        stmt.setBoolean(
                7,
                produto.isAtivo()
        );

        stmt.setLong(
                8,
                produto.getIdProduto()
        );

        return stmt.executeUpdate() > 0;

    } catch (SQLException erro) {

        throw new RuntimeException(
                "Erro ao alterar produto.",
                erro
        );
    }
}
```

---

# 8. Observação importante sobre estoque

Nesta primeira versão nós vamos permitir editar o estoque pela tela de cadastro.

Isso facilita os testes iniciais e permite cadastrar um estoque inicial.

Quando nós começarmos os movimentos de Compra e Venda, vamos evoluir essa regra.

Depois disso, o estoque deverá ser alterado principalmente por:

```text
Compra
    aumenta o estoque

Venda
    diminui o estoque
```

Ou seja, o campo de estoque deixará de ser um simples valor digitado livremente e passará a refletir as movimentações do sistema.

---

# 9. Método excluir

```java
public boolean excluir(
        long idProduto
) {

    String sql =
            "DELETE FROM produto "
            + "WHERE id_produto = ?";

    try (
            Connection conexao =
                    Conexao.conectar();

            PreparedStatement stmt =
                    conexao.prepareStatement(sql)
    ) {

        stmt.setLong(
                1,
                idProduto
        );

        return stmt.executeUpdate() > 0;

    } catch (SQLException erro) {

        throw new RuntimeException(
                "Não foi possível excluir o produto.",
                erro
        );
    }
}
```

Mais adiante, se o produto possuir compras ou vendas relacionadas, o PostgreSQL poderá impedir a exclusão.

Nesse caso, nós vamos utilizar o campo:

```text
ativo
```

para retirar o produto de uso sem apagar seu histórico.

---

# 10. Método montarProduto

```java
private Produto montarProduto(
        ResultSet rs
) throws SQLException {

    Produto produto =
            new Produto();

    produto.setIdProduto(
            rs.getLong(
                    "id_produto"
            )
    );

    produto.setDescricao(
            rs.getString(
                    "descricao"
            )
    );

    produto.setUnidade(
            rs.getString(
                    "unidade"
            )
    );

    produto.setPrecoCusto(
            rs.getBigDecimal(
                    "preco_custo"
            )
    );

    produto.setPrecoVenda(
            rs.getBigDecimal(
                    "preco_venda"
            )
    );

    produto.setEstoque(
            rs.getBigDecimal(
                    "estoque"
            )
    );

    produto.setEstoqueMinimo(
            rs.getBigDecimal(
                    "estoque_minimo"
            )
    );

    produto.setAtivo(
            rs.getBoolean(
                    "ativo"
            )
    );

    return produto;
}
```

---

# 11. Método buscarPorId

```java
public Produto buscarPorId(
        long idProduto
) {

    String sql =
            "SELECT * "
            + "FROM produto "
            + "WHERE id_produto = ?";

    try (
            Connection conexao =
                    Conexao.conectar();

            PreparedStatement stmt =
                    conexao.prepareStatement(sql)
    ) {

        stmt.setLong(
                1,
                idProduto
        );

        try (
                ResultSet rs =
                        stmt.executeQuery()
        ) {

            if (rs.next()) {

                return montarProduto(
                        rs
                );
            }
        }

    } catch (SQLException erro) {

        throw new RuntimeException(
                "Erro ao localizar produto.",
                erro
        );
    }

    return null;
}
```

---

# 12. Método listarTodos

```java
public List<Produto> listarTodos() {

    List<Produto> produtos =
            new ArrayList<>();

    String sql =
            "SELECT * "
            + "FROM produto "
            + "ORDER BY descricao";

    try (
            Connection conexao =
                    Conexao.conectar();

            PreparedStatement stmt =
                    conexao.prepareStatement(sql);

            ResultSet rs =
                    stmt.executeQuery()
    ) {

        while (rs.next()) {

            produtos.add(
                    montarProduto(
                            rs
                    )
            );
        }

    } catch (SQLException erro) {

        throw new RuntimeException(
                "Erro ao listar produtos.",
                erro
        );
    }

    return produtos;
}
```

---

# 13. Filtros da consulta

Na `FrmProduto`, nós vamos trabalhar com:

```text
DESCRICAO
ID
UNIDADE
```

Também vamos criar uma opção separada para listar produtos com estoque baixo.

---

# 14. Método pesquisar

```java
public List<Produto> pesquisar(
        String filtro,
        String pesquisa
) {

    List<Produto> produtos =
            new ArrayList<>();

    String sql;

    switch (filtro) {

        case "ID":

            sql =
                    "SELECT * "
                    + "FROM produto "
                    + "WHERE id_produto = ? "
                    + "ORDER BY descricao";

            break;

        case "UNIDADE":

            sql =
                    "SELECT * "
                    + "FROM produto "
                    + "WHERE unidade ILIKE ? "
                    + "ORDER BY descricao";

            break;

        default:

            sql =
                    "SELECT * "
                    + "FROM produto "
                    + "WHERE descricao ILIKE ? "
                    + "ORDER BY descricao";

            break;
    }

    try (
            Connection conexao =
                    Conexao.conectar();

            PreparedStatement stmt =
                    conexao.prepareStatement(sql)
    ) {

        if (
                filtro.equals("ID")
        ) {

            stmt.setLong(
                    1,
                    Long.parseLong(
                            pesquisa
                    )
            );

        } else {

            stmt.setString(
                    1,
                    "%"
                    + pesquisa
                    + "%"
            );
        }

        try (
                ResultSet rs =
                        stmt.executeQuery()
        ) {

            while (rs.next()) {

                produtos.add(
                        montarProduto(
                                rs
                        )
                );
            }
        }

    } catch (SQLException erro) {

        throw new RuntimeException(
                "Erro ao pesquisar produtos.",
                erro
        );
    }

    return produtos;
}
```

---

# 15. Método listarEstoqueBaixo

Agora nós vamos criar uma consulta específica.

```java
public List<Produto> listarEstoqueBaixo() {

    List<Produto> produtos =
            new ArrayList<>();

    String sql =
            "SELECT * "
            + "FROM produto "
            + "WHERE estoque <= estoque_minimo "
            + "AND ativo = TRUE "
            + "ORDER BY descricao";

    try (
            Connection conexao =
                    Conexao.conectar();

            PreparedStatement stmt =
                    conexao.prepareStatement(sql);

            ResultSet rs =
                    stmt.executeQuery()
    ) {

        while (rs.next()) {

            produtos.add(
                    montarProduto(
                            rs
                    )
            );
        }

    } catch (SQLException erro) {

        throw new RuntimeException(
                "Erro ao listar produtos com estoque baixo.",
                erro
        );
    }

    return produtos;
}
```

Com isso nós conseguiremos localizar produtos que precisam de reposição.

---

# 16. Criando a FrmProduto

No pacote:

```text
view
```

nós vamos escolher:

```text
New

JInternalFrame Form
```

Nome:

```text
FrmProduto
```

---

# 17. Construtor da FrmProduto

```java
public FrmProduto() {

    initComponents();

    configurarTela();

    listarProdutos();

    limparCampos();
}
```

---

# 18. Propriedades do JInternalFrame

No NetBeans:

```text
Title
Cadastro de Produtos

Closable
true

Iconifiable
true

Maximizable
true

Resizable
true
```

---

# 19. Criando o JTabbedPane

Nós vamos adicionar:

```text
JTabbedPane
```

Nome:

```text
tabProduto
```

Abas:

```text
Cadastro

Consulta
```

---

# 20. Aba Cadastro

Nós vamos criar os campos:

```text
Código
Descrição
Unidade
Preço de Custo
Preço de Venda
Estoque Atual
Estoque Mínimo
Ativo
```

Variáveis:

```text
txtCodigo
txtDescricao
cmbUnidade
txtPrecoCusto
txtPrecoVenda
txtEstoque
txtEstoqueMinimo
chkAtivo
```

---

# 21. Unidades de medida

No `cmbUnidade` nós podemos cadastrar:

```text
UN
KG
G
L
ML
M
CM
CX
PC
PCT
```

Neste projeto nós vamos manter uma lista simples.

Mais adiante poderíamos criar uma tabela específica de unidades de medida, mas neste momento isso não é necessário.

---

# 22. Botões da aba Cadastro

Nós vamos criar:

```text
Novo
Salvar
Alterar
Excluir
Cancelar
```

Variáveis:

```text
btnNovo
btnSalvar
btnAlterar
btnExcluir
btnCancelar
```

---

# 23. Aba Consulta

Nós vamos criar:

```text
Filtro
Pesquisa
Localizar
Listar Todos
Estoque Baixo
Carregar Selecionado
JTable
```

Variáveis:

```text
cmbFiltro
txtPesquisa
btnLocalizar
btnListarTodos
btnEstoqueBaixo
btnCarregar
tblProdutos
```

---

# 24. Filtros

No `cmbFiltro`:

```text
DESCRICAO
ID
UNIDADE
```

---

# 25. Colunas da JTable

Nós vamos exibir:

```text
Código
Descrição
Unidade
Preço de Custo
Preço de Venda
Estoque
Estoque Mínimo
Ativo
```

---

# 26. Imports da FrmProduto

```java
import dao.ProdutoDAO;

import java.math.BigDecimal;
import java.text.DecimalFormat;
import java.util.List;

import javax.swing.JOptionPane;
import javax.swing.table.DefaultTableModel;

import model.Produto;
import util.SessaoUsuario;
```

---

# 27. Variáveis da tela

Dentro da classe:

```java
private final ProdutoDAO produtoDAO =
        new ProdutoDAO();

private long idProdutoSelecionado = 0;

private final DecimalFormat formatoValor =
        new DecimalFormat("#,##0.00");

private final DecimalFormat formatoQuantidade =
        new DecimalFormat("#,##0.000");
```

Nós vamos utilizar os formatos apenas para apresentar os valores na tabela.

---

# 28. Método configurarTela

```java
private void configurarTela() {

    txtCodigo.setEditable(false);

    btnExcluir.setEnabled(
            SessaoUsuario.isMaster()
    );

    tblProdutos.setSelectionMode(
            javax.swing.ListSelectionModel
                    .SINGLE_SELECTION
    );

    tblProdutos.setAutoCreateRowSorter(
            true
    );

    tblProdutos.setModel(
            criarModeloTabela()
    );
}
```

---

# 29. Modelo da JTable

```java
private DefaultTableModel criarModeloTabela() {

    return new DefaultTableModel(
            new Object[]{
                "Código",
                "Descrição",
                "Unidade",
                "Preço de Custo",
                "Preço de Venda",
                "Estoque",
                "Estoque Mínimo",
                "Ativo"
            },
            0
    ) {

        @Override
        public boolean isCellEditable(
                int row,
                int column
        ) {

            return false;
        }
    };
}
```

---

# 30. Método preencherTabela

```java
private void preencherTabela(
        List<Produto> produtos
) {

    DefaultTableModel modelo =
            (DefaultTableModel)
            tblProdutos.getModel();

    modelo.setRowCount(0);

    for (
            Produto produto :
            produtos
    ) {

        modelo.addRow(
                new Object[]{
                    produto.getIdProduto(),
                    produto.getDescricao(),
                    produto.getUnidade(),
                    formatoValor.format(
                            produto.getPrecoCusto()
                    ),
                    formatoValor.format(
                            produto.getPrecoVenda()
                    ),
                    formatoQuantidade.format(
                            produto.getEstoque()
                    ),
                    formatoQuantidade.format(
                            produto.getEstoqueMinimo()
                    ),
                    produto.isAtivo()
                            ? "Sim"
                            : "Não"
                }
        );
    }
}
```

---

# 31. Método listarProdutos

```java
private void listarProdutos() {

    try {

        preencherTabela(
                produtoDAO.listarTodos()
        );

    } catch (Exception erro) {

        JOptionPane.showMessageDialog(
                this,
                "Erro ao carregar produtos.\n"
                + erro.getMessage()
        );
    }
}
```

---

# 32. Método limparCampos

```java
private void limparCampos() {

    idProdutoSelecionado = 0;

    txtCodigo.setText("");

    txtDescricao.setText("");

    cmbUnidade.setSelectedIndex(0);

    txtPrecoCusto.setText("0,00");

    txtPrecoVenda.setText("0,00");

    txtEstoque.setText("0,000");

    txtEstoqueMinimo.setText("0,000");

    chkAtivo.setSelected(true);

    txtDescricao.requestFocus();
}
```

---

# 33. Convertendo texto para BigDecimal

Como os alunos provavelmente digitarão valores utilizando vírgula, nós vamos criar um método auxiliar.

```java
private BigDecimal converterDecimal(
        String texto
) {

    String valor =
            texto
            .trim()
            .replace(".", "")
            .replace(",", ".");

    return new BigDecimal(
            valor
    );
}
```

Por exemplo:

```text
10,50
```

será convertido para:

```text
10.50
```

antes de criar o `BigDecimal`.

---

# 34. Validando os campos

```java
private boolean validarCampos() {

    if (
            txtDescricao
            .getText()
            .trim()
            .isEmpty()
    ) {

        JOptionPane.showMessageDialog(
                this,
                "Informe a descrição do produto."
        );

        txtDescricao.requestFocus();

        return false;
    }

    if (
            cmbUnidade.getSelectedItem() == null
    ) {

        JOptionPane.showMessageDialog(
                this,
                "Selecione a unidade."
        );

        cmbUnidade.requestFocus();

        return false;
    }

    try {

        BigDecimal precoCusto =
                converterDecimal(
                        txtPrecoCusto.getText()
                );

        BigDecimal precoVenda =
                converterDecimal(
                        txtPrecoVenda.getText()
                );

        BigDecimal estoque =
                converterDecimal(
                        txtEstoque.getText()
                );

        BigDecimal estoqueMinimo =
                converterDecimal(
                        txtEstoqueMinimo.getText()
                );

        if (
                precoCusto.compareTo(
                        BigDecimal.ZERO
                ) < 0
        ) {

            JOptionPane.showMessageDialog(
                    this,
                    "O preço de custo não pode ser negativo."
            );

            return false;
        }

        if (
                precoVenda.compareTo(
                        BigDecimal.ZERO
                ) < 0
        ) {

            JOptionPane.showMessageDialog(
                    this,
                    "O preço de venda não pode ser negativo."
            );

            return false;
        }

        if (
                estoque.compareTo(
                        BigDecimal.ZERO
                ) < 0
        ) {

            JOptionPane.showMessageDialog(
                    this,
                    "O estoque não pode ser negativo."
            );

            return false;
        }

        if (
                estoqueMinimo.compareTo(
                        BigDecimal.ZERO
                ) < 0
        ) {

            JOptionPane.showMessageDialog(
                    this,
                    "O estoque mínimo não pode ser negativo."
            );

            return false;
        }

    } catch (
            NumberFormatException erro
    ) {

        JOptionPane.showMessageDialog(
                this,
                "Informe valores numéricos válidos."
        );

        return false;
    }

    return true;
}
```

---

# 35. Método criarProdutoComCampos

```java
private Produto criarProdutoComCampos() {

    Produto produto =
            new Produto();

    produto.setIdProduto(
            idProdutoSelecionado
    );

    produto.setDescricao(
            txtDescricao
            .getText()
            .trim()
    );

    produto.setUnidade(
            cmbUnidade
            .getSelectedItem()
            .toString()
    );

    produto.setPrecoCusto(
            converterDecimal(
                    txtPrecoCusto.getText()
            )
    );

    produto.setPrecoVenda(
            converterDecimal(
                    txtPrecoVenda.getText()
            )
    );

    produto.setEstoque(
            converterDecimal(
                    txtEstoque.getText()
            )
    );

    produto.setEstoqueMinimo(
            converterDecimal(
                    txtEstoqueMinimo.getText()
            )
    );

    produto.setAtivo(
            chkAtivo.isSelected()
    );

    return produto;
}
```

---

# 36. Botão Novo

```java
private void btnNovoActionPerformed(
        java.awt.event.ActionEvent evt
) {

    limparCampos();

    tabProduto.setSelectedIndex(
            0
    );
}
```

---

# 37. Botão Salvar

```java
private void btnSalvarActionPerformed(
        java.awt.event.ActionEvent evt
) {

    if (
            idProdutoSelecionado != 0
    ) {

        JOptionPane.showMessageDialog(
                this,
                "Existe um produto carregado para edição.\n"
                + "Utilize Alterar ou clique em Novo."
        );

        return;
    }

    if (!validarCampos()) {

        return;
    }

    try {

        Produto produto =
                criarProdutoComCampos();

        long codigo =
                produtoDAO.cadastrar(
                        produto
                );

        JOptionPane.showMessageDialog(
                this,
                "Produto cadastrado com sucesso.\n"
                + "Código: "
                + codigo
        );

        limparCampos();

        listarProdutos();

    } catch (Exception erro) {

        JOptionPane.showMessageDialog(
                this,
                "Não foi possível cadastrar o produto.\n"
                + erro.getMessage()
        );
    }
}
```

---

# 38. Botão Localizar

```java
private void btnLocalizarActionPerformed(
        java.awt.event.ActionEvent evt
) {

    String pesquisa =
            txtPesquisa
            .getText()
            .trim();

    String filtro =
            cmbFiltro
            .getSelectedItem()
            .toString();

    if (pesquisa.isEmpty()) {

        listarProdutos();

        return;
    }

    if (
            filtro.equals("ID")
    ) {

        try {

            Long.parseLong(
                    pesquisa
            );

        } catch (
                NumberFormatException erro
        ) {

            JOptionPane.showMessageDialog(
                    this,
                    "Para pesquisar por ID informe apenas números."
            );

            txtPesquisa.requestFocus();

            return;
        }
    }

    try {

        List<Produto> produtos =
                produtoDAO.pesquisar(
                        filtro,
                        pesquisa
                );

        preencherTabela(
                produtos
        );

        if (
                produtos.isEmpty()
        ) {

            JOptionPane.showMessageDialog(
                    this,
                    "Nenhum produto encontrado."
            );
        }

    } catch (Exception erro) {

        JOptionPane.showMessageDialog(
                this,
                "Erro na pesquisa.\n"
                + erro.getMessage()
        );
    }
}
```

---

# 39. Botão Listar Todos

```java
private void btnListarTodosActionPerformed(
        java.awt.event.ActionEvent evt
) {

    txtPesquisa.setText("");

    listarProdutos();
}
```

---

# 40. Botão Estoque Baixo

```java
private void btnEstoqueBaixoActionPerformed(
        java.awt.event.ActionEvent evt
) {

    try {

        List<Produto> produtos =
                produtoDAO.listarEstoqueBaixo();

        preencherTabela(
                produtos
        );

        if (
                produtos.isEmpty()
        ) {

            JOptionPane.showMessageDialog(
                    this,
                    "Nenhum produto está com estoque baixo."
            );
        }

    } catch (Exception erro) {

        JOptionPane.showMessageDialog(
                this,
                "Erro ao consultar estoque baixo.\n"
                + erro.getMessage()
        );
    }
}
```

---

# 41. Carregando o produto selecionado

```java
private void carregarProdutoSelecionado() {

    int linha =
            tblProdutos
            .getSelectedRow();

    if (linha == -1) {

        JOptionPane.showMessageDialog(
                this,
                "Selecione um produto na tabela."
        );

        return;
    }

    int linhaModelo =
            tblProdutos
            .convertRowIndexToModel(
                    linha
            );

    long idProduto =
            Long.parseLong(
                    tblProdutos
                    .getModel()
                    .getValueAt(
                            linhaModelo,
                            0
                    )
                    .toString()
            );

    try {

        Produto produto =
                produtoDAO.buscarPorId(
                        idProduto
                );

        if (
                produto == null
        ) {

            JOptionPane.showMessageDialog(
                    this,
                    "Produto não encontrado."
            );

            return;
        }

        preencherCampos(
                produto
        );

        tabProduto.setSelectedIndex(
                0
        );

    } catch (Exception erro) {

        JOptionPane.showMessageDialog(
                this,
                "Erro ao carregar produto.\n"
                + erro.getMessage()
        );
    }
}
```

---

# 42. Método preencherCampos

```java
private void preencherCampos(
        Produto produto
) {

    idProdutoSelecionado =
            produto.getIdProduto();

    txtCodigo.setText(
            String.valueOf(
                    produto.getIdProduto()
            )
    );

    txtDescricao.setText(
            produto.getDescricao()
    );

    cmbUnidade.setSelectedItem(
            produto.getUnidade()
    );

    txtPrecoCusto.setText(
            produto
            .getPrecoCusto()
            .toPlainString()
            .replace(".", ",")
    );

    txtPrecoVenda.setText(
            produto
            .getPrecoVenda()
            .toPlainString()
            .replace(".", ",")
    );

    txtEstoque.setText(
            produto
            .getEstoque()
            .toPlainString()
            .replace(".", ",")
    );

    txtEstoqueMinimo.setText(
            produto
            .getEstoqueMinimo()
            .toPlainString()
            .replace(".", ",")
    );

    chkAtivo.setSelected(
            produto.isAtivo()
    );
}
```

---

# 43. Botão Carregar Selecionado

```java
private void btnCarregarActionPerformed(
        java.awt.event.ActionEvent evt
) {

    carregarProdutoSelecionado();
}
```

---

# 44. Duplo clique na JTable

```java
private void tblProdutosMouseClicked(
        java.awt.event.MouseEvent evt
) {

    if (
            evt.getClickCount() == 2
    ) {

        carregarProdutoSelecionado();
    }
}
```

---

# 45. Botão Alterar

```java
private void btnAlterarActionPerformed(
        java.awt.event.ActionEvent evt
) {

    if (
            idProdutoSelecionado == 0
    ) {

        JOptionPane.showMessageDialog(
                this,
                "Localize e carregue um produto antes de alterar."
        );

        return;
    }

    if (!validarCampos()) {

        return;
    }

    int resposta =
            JOptionPane.showConfirmDialog(
                    this,
                    "Deseja salvar as alterações deste produto?",
                    "Alterar Produto",
                    JOptionPane.YES_NO_OPTION
            );

    if (
            resposta
            != JOptionPane.YES_OPTION
    ) {

        return;
    }

    try {

        Produto produto =
                criarProdutoComCampos();

        boolean alterado =
                produtoDAO.alterar(
                        produto
                );

        if (alterado) {

            JOptionPane.showMessageDialog(
                    this,
                    "Produto alterado com sucesso."
            );

            limparCampos();

            listarProdutos();

        } else {

            JOptionPane.showMessageDialog(
                    this,
                    "Nenhum registro foi alterado."
            );
        }

    } catch (Exception erro) {

        JOptionPane.showMessageDialog(
                this,
                "Não foi possível alterar o produto.\n"
                + erro.getMessage()
        );
    }
}
```

---

# 46. Botão Excluir

```java
private void btnExcluirActionPerformed(
        java.awt.event.ActionEvent evt
) {

    if (
            !SessaoUsuario.isMaster()
    ) {

        JOptionPane.showMessageDialog(
                this,
                "Você não possui permissão para excluir produtos."
        );

        return;
    }

    if (
            idProdutoSelecionado == 0
    ) {

        JOptionPane.showMessageDialog(
                this,
                "Localize e carregue um produto antes de excluir."
        );

        return;
    }

    int resposta =
            JOptionPane.showConfirmDialog(
                    this,
                    "Deseja realmente excluir este produto?",
                    "Excluir Produto",
                    JOptionPane.YES_NO_OPTION
            );

    if (
            resposta
            != JOptionPane.YES_OPTION
    ) {

        return;
    }

    try {

        boolean excluido =
                produtoDAO.excluir(
                        idProdutoSelecionado
                );

        if (excluido) {

            JOptionPane.showMessageDialog(
                    this,
                    "Produto excluído com sucesso."
            );

            limparCampos();

            listarProdutos();

        } else {

            JOptionPane.showMessageDialog(
                    this,
                    "Produto não encontrado."
            );
        }

    } catch (Exception erro) {

        JOptionPane.showMessageDialog(
                this,
                "Não foi possível excluir o produto.\n"
                + "Ele pode possuir compras ou vendas vinculadas.\n"
                + erro.getMessage()
        );
    }
}
```

---

# 47. Controle de permissão

Nós teremos novamente duas verificações.

Na configuração da tela:

```java
btnExcluir.setEnabled(
        SessaoUsuario.isMaster()
);
```

E novamente dentro do evento:

```java
if (
        !SessaoUsuario.isMaster()
)
```

Assim:

```text
MASTER
    ↓
Pode excluir
```

```text
USER
    ↓
Não pode excluir
```

---

# 48. Botão Cancelar

```java
private void btnCancelarActionPerformed(
        java.awt.event.ActionEvent evt
) {

    limparCampos();
}
```

---

# 49. Ligando FrmProduto à FrmPrincipal

Agora nós vamos ativar:

```text
Cadastros
    Produtos
```

Na `FrmPrincipal`:

```java
private void mnuProdutosActionPerformed(
        java.awt.event.ActionEvent evt
) {

    abrirTela(
            new FrmProduto()
    );
}
```

---

# 50. Fluxo do cadastro

```text
FrmPrincipal
    ↓
Cadastros
    ↓
Produtos
    ↓
FrmProduto
    ↓
Aba Cadastro
    ↓
Salvar
    ↓
Produto
    ↓
ProdutoDAO
    ↓
PostgreSQL
```

---

# 51. Fluxo da consulta

```text
Aba Consulta
    ↓
Escolher filtro
    ↓
Digitar pesquisa
    ↓
Localizar
    ↓
ProdutoDAO
    ↓
SELECT
    ↓
JTable
```

---

# 52. Fluxo da alteração

```text
Consulta
    ↓
Selecionar produto
    ↓
Carregar Selecionado
    ↓
buscarPorId
    ↓
Cadastro
    ↓
Alterar
    ↓
UPDATE
```

---

# 53. Fluxo da consulta de estoque baixo

```text
Aba Consulta
    ↓
Estoque Baixo
    ↓
ProdutoDAO
    ↓
estoque menor ou igual ao estoque mínimo
    ↓
JTable
```

---

# 54. Teste de cadastro

Nós podemos cadastrar:

```text
Descrição:
Furadeira de Impacto 650W

Unidade:
UN

Preço de Custo:
180,00

Preço de Venda:
279,90

Estoque:
10,000

Estoque Mínimo:
3,000

Ativo:
Sim
```

Depois clicamos:

```text
Salvar
```

O produto deverá aparecer na aba Consulta.

---

# 55. Segundo produto de teste

Podemos cadastrar:

```text
Descrição:
Parafuso Sextavado 8mm

Unidade:
UN

Preço de Custo:
0,35

Preço de Venda:
0,80

Estoque:
500,000

Estoque Mínimo:
100,000

Ativo:
Sim
```

Esse exemplo mostra por que utilizamos casas decimais mesmo em produtos simples.

---

# 56. Testando filtro por descrição

Na consulta:

```text
Filtro:
DESCRICAO
```

Pesquisa:

```text
Furadeira
```

Depois:

```text
Localizar
```

A tabela deverá mostrar os produtos encontrados.

---

# 57. Testando pesquisa parcial

Pesquise:

```text
Impacto
```

Nós também deveremos encontrar:

```text
Furadeira de Impacto 650W
```

Isso ocorre porque utilizamos:

```sql
ILIKE
```

junto com os caracteres `%`.

---

# 58. Testando estoque baixo

Cadastre ou altere um produto para:

```text
Estoque:
2,000

Estoque Mínimo:
5,000
```

Depois clique:

```text
Estoque Baixo
```

Esse produto deverá aparecer na tabela.

---

# 59. Testando alteração

Selecione um produto.

Clique:

```text
Carregar Selecionado
```

Altere o preço de venda.

Depois clique:

```text
Alterar
```

A consulta deverá mostrar o novo valor.

---

# 60. Testando exclusão com MASTER

Entre como:

```text
master
```

Carregue um produto.

O botão:

```text
Excluir
```

deverá estar habilitado.

---

# 61. Testando exclusão com USER

Faça logout.

Entre como:

```text
user
```

Abra a `FrmProduto`.

O botão:

```text
Excluir
```

deverá ficar desabilitado.

---

# 62. Produto inativo

Em sistemas comerciais, muitas vezes nós não queremos excluir produtos que já possuem movimentações.

Nós podemos apenas retirar a marcação:

```text
Ativo
```

Assim o registro continuará existindo no histórico, mas poderá ser impedido de aparecer em novas compras ou vendas.

Mais adiante nós vamos utilizar essa regra nos movimentos.

---

# 63. Estoque atual e estoque mínimo

Nós temos dois conceitos diferentes.

`estoque` representa quanto existe atualmente.

`estoque_minimo` representa a quantidade mínima desejada.

Exemplo:

```text
Estoque atual:
3

Estoque mínimo:
5
```

Isso significa que o produto está abaixo da quantidade desejada.

Nossa consulta:

```sql
WHERE estoque <= estoque_minimo
```

permite localizar esses produtos automaticamente.

---

# 64. Preparando o estoque para Compras

Quando desenvolvermos a Compra, nós vamos trabalhar com:

```text
Estoque atual
    +
Quantidade comprada
    =
Novo estoque
```

Exemplo:

```text
Estoque atual:
10

Compra:
5

Novo estoque:
15
```

Essa atualização será automática.

---

# 65. Preparando o estoque para Vendas

No movimento de Venda nós teremos o processo contrário.

```text
Estoque atual
    menos
Quantidade vendida
    =
Novo estoque
```

Exemplo:

```text
Estoque atual:
15

Venda:
4

Novo estoque:
11
```

Também vamos verificar se existe estoque suficiente antes de concluir a venda.

---

# 66. Métodos principais da FrmProduto

Nossa tela terá:

```text
configurarTela
criarModeloTabela
preencherTabela
listarProdutos
limparCampos
converterDecimal
validarCampos
criarProdutoComCampos
carregarProdutoSelecionado
preencherCampos
```

---

# 67. Métodos da ProdutoDAO

Nossa DAO terá:

```text
cadastrar
alterar
excluir
buscarPorId
listarTodos
pesquisar
listarEstoqueBaixo
montarProduto
```

---

# 68. Padrão arquitetural

Agora nós temos:

```text
Cliente
    ↓
ClienteDAO
    ↓
FrmCliente
```

```text
Fornecedor
    ↓
FornecedorDAO
    ↓
FrmFornecedor
```

```text
Produto
    ↓
ProdutoDAO
    ↓
FrmProduto
```

Nós estamos mantendo o mesmo padrão em todo o sistema.

---

# 69. Resultado desta etapa

Depois da `FrmProduto`, nosso FerragemGK já terá:

```text
Login

Controle MASTER e USER

FrmPrincipal

JDesktopPane

JInternalFrame

Cadastro de Clientes

Cadastro de Fornecedores

Cadastro de Produtos

JTabbedPane

JTable

Filtros

Pesquisa parcial

Controle de estoque

Estoque mínimo

BigDecimal

INSERT

SELECT

UPDATE

DELETE
```

---

# 70. Próxima etapa

Depois do cadastro de produtos, nós já teremos as três informações necessárias para iniciar o primeiro movimento comercial:

```text
Fornecedor
Produto
Usuário conectado
```

Na próxima etapa nós poderemos começar a criar:

```text
FrmCompra
```

Nesse módulo nós vamos trabalhar com:

```text
Fornecedor
Produtos da Compra
Quantidade
Valor de custo
Subtotal
Total da compra
Forma de pagamento
Parcelamento
Entrada automática no estoque
Contas a pagar
Transação
commit
rollback
```

Essa será uma mudança importante no projeto porque deixaremos de trabalhar apenas com CRUD e começaremos a desenvolver regras de negócio envolvendo várias tabelas ao mesmo tempo.
